tmpclaude-*
nul
.claude/.personality_migration
.claude/auth.json
.claude/history.jsonl
/.claude/log
.claude/models_cache.json
/.claude/sessions
/.claude/skills/.system
.claude/tmp/arg0/codex-arg0HLLJ1f/.lock
/.claude/tmp
.claude/version.json

# Secrets sync
.secrets
.secrets-download/
.secrets-decrypted.tmp
# Backups taken before a sync. Deliberately not `.secrets*` — that would also
# match the committed .secrets.example.
.secrets.bak*

# Chat transcripts (may contain pasted secrets)
latest-chat.log

# Node / Wrangler build artifacts
node_modules/
.wrangler/
cloudflared/reports-*/

# Playwright artifacts from cloudflared/test-console.mjs
cloudflared/console-loaded.png
cloudflared/console-presets.png
